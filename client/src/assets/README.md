# Static Assets Directory

Place global brand assets, SVG logos, illustration vectors, and event badge icons here.
Assets imported directly in TypeScript / JSX files should live here.
Static public assets served as-is (e.g. `favicon.ico`) live in `client/public/`.
